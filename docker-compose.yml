version: "3.8"

services:

  mysql:
    image: mysql:8.0
    container_name: mysql-container

    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: mydb

    ports:
      - "3306:3306"

  flaskapp:
    build: .
    container_name: flask-container

    ports:
      - "5000:5000"

    depends_on:
      - mysql
